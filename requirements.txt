# Web framework
fastapi>=0.100.0
uvicorn[standard]>=0.30.0
sse-starlette>=0.10.0
python-multipart>=0.0.20

# LangChain / LangGraph
langchain>=0.2.0
langchain-openai>=0.2.0
langgraph>=0.2.0
langchain-core>=0.2.0
langgraph-checkpoint-postgres>=2.0.0

# Database
psycopg[binary,pool]>=3.1.8

# Vector store
# On Windows without MSVC, install separately first:
#   pip install chromadb --prefer-binary
chromadb>=0.5.0

# Config / validation
pydantic>=2.5.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
python-docx>=1.0.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.25.0
httpx>=0.27.0
